# v0.1.0 — initial public release

**FactorTail** is the reference implementation of the manuscript
*"Conditional Monte Carlo for Multivariate Heavy Tails: Latent Shocks,
Spectral Measures, Hidden Cones"* by O. Shams Solari and F. Pourbabaee.

This is the first public release.

## Headline numbers

- **54 source modules** (`src/` layout), MIT-licensed.
- **29 generated artifacts** with schema-validated CSVs and PDFs.
- **136 mathematical-correctness tests** (unit + integration + hypothesis property-based).
- **mypy clean** on the entire library.
- **6 priority runs (P1–P6)** pass the App. G replacement contract.

## What's in the package

### CdMC estimators (Sections 3–5)

- `factortail.cdmc.independent.independent_cdmc` — §3 baseline with the
  $N^\alpha - 1$ BRE bound and Bernstein CIs.
- `factortail.cdmc.dependent.dependent_cdmc` — arbitrary-dependence
  identity (`alg:dep-cdmc`) with optional batched-kernel fast path.
- `factortail.cdmc.latent_shock.latent_shock_cdmc` — `alg:latent-cdmc`.
- `factortail.cdmc.block.block_cdmc` + `fit_block_tail` —
  `thm:block-reduction` with closed-form and nested-MC tail fitters.
- `factortail.cdmc.spectral.spectral_cdmc` — `alg:spectral-cdmc`.
- `factortail.cdmc.copula_kernel.build_copula_kernel` /
  `build_copula_kernel_batched` — turn any copula into a kernel for
  `dependent_cdmc`.

### Copulas (§4)

- Gaussian, Student-t (any dimension); Clayton in any dimension via the
  closed-form Archimedean conditional CDF; Gumbel / Frank in d=2 with
  conditional survival; C-vine truncation for d > 2.

### DGPs (§8, Families I–VI)

- `IndependentINID` — Pareto / Lomax / Burr / Student-t with signed
  exposures.
- `CommonShockModel`, `LatentFactorModel` — Family II.
- `BlockModel` — Family III.
- `CopulaModel` — Family IV.
- `RadialAngularMRV` — Family V with axis / ray-mixture / Dirichlet /
  empirical angular components.
- `HiddenConeMixture` — Family VI.

### Hidden RV (§6)

- `ledford_tawn_eta` — Hill-based residual tail-dependence.
- `cone_mass_alpha` — direct cone-mass slope estimator (recovers $\alpha_2$
  with 9 % error on the Family VI design vs Ledford–Tawn's 49 % error).
- `hrv_mixture_estimator` — stratified axis + hidden-cone mixture
  (Theorem `thm:hidden-second-order`).

### Estimator families (§7)

- `control_variate` — oracle and sample-split VRE (Proposition `prop:vre`).
- `spectral_control_variate` — coupled spectral CV.
- Bernstein CI (Theorem `thm:bernstein-ci`).

### Diagnostics

- Hill / Pickands / POT-GPD tail-index estimators.
- Pairwise $\chi$ / $\bar\chi$ / $\eta$ matrices.
- Empirical spectral measure with iid / block / stationary bootstrap
  bands.

### Real-data pipeline (§9)

- Fama–French loader with online + offline-synthetic fallback.
- Rolling VaR/ES (`alg:real-data`).
- Kupiec / Christoffersen / dynamic-quantile / Acerbi–Szekely Z2.

### Reproducibility

- PCG-style replicate seeds via `factortail.utils.seeds.SeedSpawner`.
- Config-hash provenance, git-hash stamping on every output.
- App. G replacement-contract enforcement (`factortail validate-run`).

## Generated artifacts

| Layer | Outputs | Source script |
|---|---|---|
| §3 baseline | `F1` (tail equivalence), `F2` (max vs sum), `F3`–`F5` (efficiency rates), `F6` (VRE pilot benchmark), `F7` (stratified), `F8` (second order), `T_sim_results_independent` | `scripts/generate_F{1..8}*.py`, `scripts/generate_T_sim_results_independent.py` |
| §4 latent / block / copula | `F11` (common-shock geometry), `T_sim_results_dependent` | `scripts/generate_F11*.py`, `scripts/generate_T_sim_results_dependent.py` |
| §5 spectral MRV | `F12` (simplex), `F19` (bootstrap audit) | `scripts/generate_F12*.py`, `scripts/generate_F19*.py` |
| §6 hidden RV | `F13` (hidden cones) | `scripts/generate_F13*.py` |
| Dashboard | `F14` | `scripts/generate_F14*.py` |
| §9 real data | `F9`/`F10` (per-portfolio VaR/backtest), `F15` (tail-dep heatmap), `F16` (VaR/ES dashboard), `F17` (rolling spectral), `F18` (Hill plots), `T_data_panels`, `T_tail_index_placeholder`, `T_dependence_diagnostic_placeholder`, `T_var_es_backtest_placeholder`, `T_crisis_attribution_placeholder`, `T_runtime_placeholder`, `T_realdata_experiments`, `T_empirical_design_matrix` | `scripts/generate_F{9,10,15..18}*.py`, `scripts/generate_T_*.py` |

## Handoff open questions — closed

- **Q1 (pilot size).** Sample-split VRE on Family I with
  $n_0 \in \{\sqrt n,\, n/\log n,\, n^{2/3}\}$. Result: under identical
  Pareto margins the marginal surrogate has $\rho \approx 0$, so all
  pilot rules tie. Future work needs a replicate-varying surrogate;
  `spectral_control_variate` shows $\rho > 0$.
- **Q2 (hidden-tail scale).** Direct cone-mass slope estimator
  outperforms Ledford–Tawn $\eta$ on Family VI (8.8 % vs 49 % relative
  error at $\alpha_2 = 3$).
- **Q3 (vine integration tolerances).** `CVineKernel` truncates to
  bivariate Archimedean pairs; exact at d=2; tolerance set by the
  underlying bivariate kernel.
- **Q4 (block vs stationary bootstrap).** On AR(1)-injected
  Pareto-Dirichlet MRV, all three schemes (iid / block / stationary)
  achieve full coverage of the true spectral constant; iid produces
  the narrowest bands (overconfident under serial dependence).

## Tooling

- PEP 621 `pyproject.toml`, `src/` layout.
- CI matrix: Python 3.10–3.12 × Linux / macOS, lint + type + tests.
- Docs auto-deployed to `gh-pages` on every push to `main`.
- Pre-commit: ruff, black, codespell, pyupgrade, schema cross-check.
- `Makefile`, `tox.ini`, `mkdocs.yml`.

## Install

```bash
pip install factortail              # once on PyPI
pip install factortail[plot,docs]   # with optional extras
```

## Cite

See [`CITATION.cff`](https://github.com/osolari/FactorTail/blob/main/CITATION.cff) for the canonical citation block.

## Next

See [`PLAN.md`](https://github.com/osolari/FactorTail/blob/main/PLAN.md) for the Tier-D bonus
items (Numba acceleration, JAX/GPU backend, `factortail repro` CLI,
tutorial notebooks).
